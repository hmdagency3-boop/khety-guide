// Support routes have been migrated to Supabase.
// All support chat and location operations are now handled
// directly by the frontend using the Supabase client.
// This file is kept as a placeholder to avoid import errors.

import { Router, type IRouter } from "express";
const router: IRouter = Router();
export default router;
