{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:frontend": "pnpm --filter @workspace/khety run build",
    "build:api": "pnpm --filter @workspace/api-server run build",
    "build:libs": "pnpm -r --filter \"./lib/**\" --if-present run build",
    "build:all": "pnpm run build:libs && pnpm run build:frontend && pnpm run build:api",
    "dev:frontend": "pnpm --filter @workspace/khety run dev",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "typecheck:libs": "tsc --build",
    "typecheck:frontend": "pnpm --filter @workspace/khety run typecheck",
    "typecheck:api": "pnpm --filter @workspace/api-server run typecheck",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.3.0",
    "stripe": "^21.0.1",
    "stripe-replit-sync": "^1.0.0"
  },
  "pnpm": {
    "overrides": {
      "serialize-javascript": ">=7.0.3"
    }
  }
}
